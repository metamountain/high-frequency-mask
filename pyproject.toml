[project]
name = "high-frequency-mask"
description = "Builds a mask from image texture: white where there is detail, black where the image is flat."
version = "0.1.0"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = []

[project.urls]
Repository = "https://github.com/OWNER/high-frequency-mask"

[tool.comfy]
PublisherId = ""
DisplayName = "High Frequency Mask"
Icon = ""
